let myValue = "Hello"; // You can change this value to any data type

if (typeof myValue === "string") {
  myValue += " World!";    // if it's a string
  alert(myValue);


} else {
  alert("The value is not a string.");
}